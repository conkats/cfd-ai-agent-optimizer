Create an agent team to generate a report of comparing the effects of different shapes on the flow domain as in and using the template claude-test. Use 5 teammates in the exact sequence and place all results, cases, report and scripts in the directory agent-orchestration-claude:

- Teamate1: Call the  web-research-orchestrator and read how the different shapes and plots are generated for comparing the CP coefficient, velocity magnitude, drag and lift and the topic of shape optimisation topic.
- Teamate2: Call the saturne-mesh-design and use the cylinder_crossflow.py to generate a new script that have 4 different shapes, square, cylinder, half cylinder and triangle which can be parametrised and using Teammate 1's research. The domain should be kept 2D and same dimension, width and length as in cylinder_crossflow.py.
- Teammate3: Use the script frmo Teamate2, call cfd-case-runner and generate four code_saturne cases using the same conditions /home/katsa1k/cfd-work/saturne-tutorials/13_Fluid_Structure_Interaction/claude-test/RESU/20260519-0925, run for 2000 iterations, e.g. the cylinder case, square case, half cylonder, triangle cases but for each case have a separate directory. Once all are completed, use plot_fsi_mesh_comparison.py as a template to generate a new script for the comparison of the results. Generate some velocity contours to compare the effects of the different shapes. 
- Teammate4: Call the descriptor agent and generate a 2 page report of the results and findings. Have a small section at the top to describe how to run different shape and use the scipt if I want to run more cases manually.
- Teamate5: Use the code-reviewer to simplify and optimise the scripts for the mesh and results where necessary.
